Agent Directive: Fix Incorrect API Fallback & Mapping
The Situation: You implemented a fallback to filters[area] because the code thinks the project relation doesn't exist. This is causing a 400 error and a "Not Found" UI. We need to stop the fallback and fix the actual relationship mapping.

1. Stop the "Area" Fallback:

Locate the logic in properties.ts (around line 38 and 76 based on the logs).

Remove the fallback logic that switches project to area. This is a "hallucinated" fix that is breaking the query.

2. Identify the Correct Relation Name:

Do not guess the name. Look at the Property content-type schema in Strapi.

Is the relation named project, project_link, or projects?

Update the query string to use the exact internal name from Strapi.

3. Fix the "Project Name" Path:

Strapi filtering for relationships usually requires a deeper path. If project is the relation, ensure the query is:
filters[project][name][$eq]=Downtown Views

If the project's display name is in a different field (like title), use:
filters[project][title][$eq]=Downtown Views

4. Force Data Validation:

Before sending the fetch request, console.log the final generated URL.

If the URL doesn't work when pasted directly into a browser tab, it won't work in the app.