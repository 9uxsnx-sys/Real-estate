Task: The PropertiesListing component is crashing (White Screen) when fetching more data. You must fix the data transformation logic, register missing GSAP plugins, and handle 400 Bad Request errors.

1. Fix the Fatal Crash (formatters.ts & imagePath)
The Error: Uncaught TypeError: imagePath.startsWith is not a function.

The Cause: getImageUrl expects a string, but when you load more properties, it’s receiving null, undefined, or an object.

Action: Open formatters.ts and update getImageUrl to include a safety check.

Logic: If imagePath is missing or not a string, return a placeholder image string or an empty string instead of crashing.

Check: Ensure the mapping in PropertiesListing.tsx:128 passes the correct nested path (e.g., item.attributes.image.data.attributes.url).

2. Fix the API 400 (Bad Request)
The Error: .../api/properties?filters[project][name][$eq]=... 400 (Bad Request).

The Cause: The URL encoding for the project filter is likely malformed or the Strapi field name project doesn't match the relation name in the backend.

Action: * Log the exact URL being generated for "Load More."

Verify that the relation in Strapi is named project and not projects.

Ensure the agent uses a robust query builder (like qs) or correctly encodes spaces in project names (e.g., "Downtown Views").

3. Fix GSAP "Missing Plugin" & "Target Null"
The Error: Invalid property scrollTrigger... Missing plugin?.

Action: * Search the codebase for gsap.registerPlugin. If it's missing ScrollTrigger, add it: gsap.registerPlugin(ScrollTrigger);.

Crucial: The "Target null" error is happening because GSAP is trying to animate elements that vanished when the React app crashed. Fixing the Image error (Task 1) will likely solve this, but add a check: if (!target) return; before any animation.

4. Full Picture Diagnostic Report
Agent, please provide a report covering:

Data Structure: What does the JSON response look like for the "Load More" properties? (Print it to console).

Mapping: Why did imagePath fail? Was the image missing in Strapi, or was the nesting level different?

Fix Applied: Show the updated getImageUrl and the fixed Fetch URL.