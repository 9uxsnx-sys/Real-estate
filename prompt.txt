Issue: The filter filters[project][name][$eq]=... is failing. You must verify the exact schema path instead of using fallbacks like area.

1. Identify the Correct Field Path:

Don't guess: Check the Project content type in the Strapi Admin.

Is the main text field actually named name? If it is named title, the filter MUST be changed to filters[project][title][$eq]=....

If the relationship itself is plural, it MUST be filters[projects][name][$eq]=....

2. Audit the Query String Construction:

Your logs show Downtown%20Views. Ensure you are using the qs library with { encodeValuesOnly: true }. Strapi can be very sensitive to how brackets [] are encoded in nested filters.

Update the fetcher:

JavaScript
// Ensure the query looks like this for Strapi v4/v5
const query = qs.stringify({
  filters: {
    project: { // Verify if this should be 'projects'
      title: { // Verify if this should be 'name'
        $eq: selectedProjectName
      }
    }
  },
  populate: '*'
});
3. Deep-Dive the 400 Error:

In api.ts:27, expand the [API Error] object in the console.

Look for response.data.error.details. It will likely say something like: "Column 'name' not found" or "Relation 'project' not found." * Use that exact feedback to rename the filter keys.

4. Permission Check:

Confirm that the Project content type has find and findOne enabled for the Public role. If the related type isn't public, filtering by its attributes will fail with a 400.