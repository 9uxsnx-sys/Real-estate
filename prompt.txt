Agent Directive: STOP Area Mapping & Force Native Relation Filter
Context: The manual "Project to Area" mapping is now preventing the website from working. I have verified that the relation is visible in the Strapi Admin. We must stop using filters[area] for project selections.

Task 1: Delete the Mapping Hack

Locate the projectToAreaMap constant in properties.ts. Delete it.

Locate the logic that says [API] Mapping project to area. Delete it.

Constraint: Do not allow the code to "fall back" to area. If a project is selected, the query must target the project relationship.

Task 2: Fix the 400 Bad Request (The "Deep Filter" Syntax)

The log shows: filters[project][name][$eq]=... is failing with a 400.

Action: Change the filter syntax to the Strapi v4/v5 standard for relations. Try this exact structure:
filters[project][name][$eq]=Garden Heights

If that still returns 400: Try changing the key to plural: filters[projects][name][$eq]=Garden Heights.

Important: Use the qs library to stringify the query. Manual string templates often break Strapi's bracket parsing.

Task 3: Ensure "Populate" is Correct

Ensure every request includes populate=*. Without this, Strapi won't "look" into the relationship to find the name you are filtering for.

Task 4: Clear the "Not Found" UI

The "Not Found" screen is appearing because the area filter is looking for "Jumeirah Golf Estates" in the area column, finding nothing, and returning an empty array.

Once the filter is corrected to target the project relation, the data will return, and the UI will show the property cards.