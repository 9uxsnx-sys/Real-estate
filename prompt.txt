Agent Directive: Remove Fallbacks & Switch to Native Project Filtering
The Situation: I have successfully linked the properties to projects in the Strapi Admin. The "Project" field is now visible and populated. However, the website shows "Not Found" because the code is still using the area fallback.

Task 1: Delete the Fallback Logic

Open properties.ts and api.ts.

Remove the projectToAreaMap.

Remove the logic that switches filters[project] to filters[area].

Task 2: Implement Native Relationship Filter

Update the fetch logic so that when a project is selected, it uses the correct Strapi deep-filtering syntax:
?filters[project][name][$eq]=Garden Heights&populate=*

Ensure the populate=* is always present so the project data is included in the response.

Task 3: Verify Data Mapping

The "Not Found" screen appears when the properties array is empty.

Once you update the filter, console.log the API response.

Ensure the frontend is reading the property ID and attributes correctly from the new relationship-based response.

Task 4: Clear GSAP Targets

Since the "Not Found" state removes the property cards from the DOM, ensure GSAP is not trying to animate them until the properties.length > 0.