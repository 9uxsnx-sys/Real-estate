Agent Directive: Fix Desynced Strapi Schema & Relation Errors
Context: The "Invalid key project" error proves the SQLite database is desynced from the JSON schema. The current projectToAreaMap is a temporary patch that must be removed once the database is fixed.

Task 1: Force Database Schema Sync

Stop the Strapi server.

Run Development Mode: Run npm run develop (not build). This forces Strapi to check the schema.json and attempt to alter the database tables to add the missing project_id columns.

Verify the Column: Check the Strapi Admin UI. Open a Property; does the "Project" dropdown actually show up and allow you to select a project?

Task 2: Data Migration (The "Re-Save" Fix)

The Problem: Existing properties don't have a project assigned.

The Action: Since there are only a few projects, go into the Strapi Admin Panel and manually edit the properties.

Select the correct Project for each Property.

Click Save and Publish.

Note: If there are too many properties, the agent should write a one-time script to iterate through /api/properties and PUT the correct project ID based on the report's ID list.

Task 3: Clean up Frontend "Workaround"

Once Task 1 & 2 are done, delete the projectToAreaMap from properties.ts.

Revert the filtering logic to use the real relationship:
filters[project][name][$eq]=...

Ensure populate=project is included in the fetch call.

Task 4: Final Validation

Confirm that the PUT /api/properties/33 request no longer returns "Invalid key project.